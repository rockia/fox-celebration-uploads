import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fox Celebration Uploads",
  description: "A demo file upload application with real-time progress tracking.",
  keywords: ["file upload", "demo", "react", "nextjs", "typescript",],
  authors: [{ name: "Fox Celebration Uploads Demo" }],
  icons: {
    icon: "/fox-favicon.svg",
    shortcut: "/fox-favicon.svg",
    apple: "/fox-favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
